import { WelcomeScreen } from './screens';
import {
  SafeAreaView,
} from 'react-native-safe-area-context';

export default function App() {
  return (
    <SafeAreaView>
      <WelcomeScreen/>
    </SafeAreaView>
  );
}
